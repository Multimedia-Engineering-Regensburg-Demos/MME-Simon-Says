/* eslint-env browser */

var SimonSays = SimonSays || {};

SimonSays.App = (function() {
  "use strict";

  var that = {};

  function init() {
  }

  that.init = init;
  return that;
}());

SimonSays.App.init();